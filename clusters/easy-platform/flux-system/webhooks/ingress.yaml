apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flux-webhooks
  namespace: flux-system
  annotations:
    platform.lazycampus.com/domain-automation: enabled
    platform.lazycampus.com/domain-reconcile-request: "2026-09-11T06:14:00Z"
spec:
  ingressClassName: traefik
  rules:
    - host: hooks.lazycampus.com
      http:
        paths:
          - path: /hook/
            pathType: Prefix
            backend:
              service:
                name: webhook-receiver
                port:
                  name: http
